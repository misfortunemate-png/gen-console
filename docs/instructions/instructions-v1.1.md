# gen-console 作業指示書（v1.1）

作成日: 2026-07-25 ／ PM: クリーデ ／ 対応仕様: docs/spec-v1.1.md
位置づけ: 本書一枚でv1.1の全指示が完結する（追補なし・R-014）

## 添付マニフェスト（着工前照合・必須）

以下がすべて交換所（リポジトリ）に存在すること。**1つでも欠けたら着工せず docs/reports/ に報告。**

| # | パス | 種別 | SHA-256（支給物のみ） |
|---|---|---|---|
| 1 | docs/spec-v1.1.md | 仕様書 | — |

支給物はなし。

## PG運用規律（定型・全フェーズ共通）

1. **三則**: ①難航時はPMへ差し戻す ②原因判明時は「原因X・対策Y・実行可否」で報告→指示待ち ③セッション外プロセス停止等は事前許可
2. **宛先**:
   - **仕様の疑義・技術判断** → docs/reports/ にpushしてPMへ
   - **環境・インフラの問題**（ファイルが見つからない、権限、起動不能等）→ 発注者に直接聞いてよい
   - **実機試験の依頼・承認** → 発注者
3. **支給物改変禁止**: 本フェーズ支給物なし
4. **着工前**: `git pull` → inspect実行（マニフェスト照合・版確認）。inspect未整備のため、最初の作業として§0でscripts/inspect.mjsを作成してから着工判定を行う
5. **完了宣言禁止**: inspect緑を添えて「確認をお願いします」で止める

## 作業範囲

- 何を: 仕様書§0〜§4（inspect整備／出力一元化／拡大表示／モデル・LoRA可視化／LoRAローダー）
- なぜ: 成果物の散在解消と生成条件の把握性向上（要件承認 2026-07-25）
- どこで: gen-console リポジトリ（D:\AI\imagegen または D:\AI\github\gen-console のclone実体。現物のパスは_STATUS.md／start.batの前提に従う）

## 作業手順

1. `git pull` → docs/spec-v1.1.md を読む
2. §0: scripts/inspect.mjs 新設 → inspect実行（マニフェスト照合・版確認）
3. §1 → §3 → §4 → §2 の順に実装（§2はUI独立のため最後でよい。順序変更はPG裁量可、コミットは§単位・5W1H）
4. package.json を 1.1.0 に更新し、/healthz 応答と一致させる（R-012）
5. 自己テスト（仕様§5のPG自己完結分）→ inspect緑 → push → _STATUS.md更新

## 禁止事項

- `content\` 配下の**内容**の読み取り（CLAUDE.md既定。ファイル名・件数・サイズの機械的操作は可）
- ComfyUI本体（D:\AI\imagegen\ComfyUI）の設定・カスタムノード・モデル配置の変更（環境整理は別指示で実施中。ワークフローJSONは本リポジトリ管理分のみ変更可）
- 追加npm依存の導入（必要と判断した場合は実装せず docs/reports/ へ提案）
- 仕様外のファイル構成変更（R-006）

## テスト

- PG自己完結分: 仕様§5のうち「§1保存・台帳」「§1画像配信」「§3表示」「§4空一覧」「版確認」
- **実機系（発注者に依頼）**:
  1. Pixel 10とフランのブラウザでのライトボックス操作（ピンチ/ホイールズーム・パン・閉じる）
  2. LoRA導入後の実適用生成（強度反映の確認）※LoRA未導入の間はこの項目を保留とし、保留のまま検査に進んでよい

## 完了条件

- 仕様§0〜§4の実装完了、PG自己完結テスト合格
- inspect緑・push済み・_STATUS.md更新（30行以内）・5W1Hコミット
- 未実施の実機系テストを_STATUS.mdに「未検証項目」として明記（#012）
